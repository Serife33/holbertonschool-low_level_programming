#include <stdio.h>
#include "main.h"
/**
 * mul - multiply number.
 *
 * @a: the character to multiply
 *
 * @b: the character to multiply
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
